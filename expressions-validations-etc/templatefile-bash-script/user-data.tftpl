#!/bin/bash

echo "Starting setup for ${environment}"

apt update -y
apt install -y nginx

echo "<h1>Hello from ${environment}</h1>" > /var/www/html/index.html

%{ if enable_extra_message }
echo "<p>Extra message: ${extra_message}</p>" >> /var/www/html/index.html
%{ endif }